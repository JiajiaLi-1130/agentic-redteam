---
name: combine-skills
description: Draft a new skill concept by combining existing configured skills.
metadata:
  version: "1.0"
---

# combine-skills

This meta-skill combines configured skills into a new draft concept.

Example:

- emoji disguise + rewrite framing
- memory-informed rewrite strategy + model-backed generation settings

The output is a draft spec idea, not an executable new skill directory.
