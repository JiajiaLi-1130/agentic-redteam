{
  "name": "rewrite_language",
  "type": "rewrite_skill",
  "inputs": {
    "base_prompt": "string",
    "num_candidates": "integer",
    "config": "object"
  },
  "outputs": {
    "rewrites": [
      {
        "text": "string",
        "strategy": "language",
        "metadata": "object"
      }
    ]
  }
}